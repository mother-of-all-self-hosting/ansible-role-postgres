# SPDX-FileCopyrightText: 2026 spatterlight
# SPDX-FileCopyrightText: 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Verify Postgres systemd service
  hosts: all
  become: true
  gather_facts: false

  vars:
    postgres_verify_units:
      - postgres.service

  tasks:
    - name: Verify that Postgres works
      block:
        - name: Wait for the Postgres systemd services to become active (up to 180s each)
          ansible.builtin.systemd_service:
            name: "{{ item }}"
          register: postgres_services
          until: postgres_services.status.ActiveState == 'active'
          retries: 36
          delay: 5
          loop: "{{ postgres_verify_units }}"
          # So that giving up is reported by the assertion below rather than as a
          # wall of systemd unit properties.
          failed_when: false

        # `Restart=always` in the systemd unit means that even a container which crashes
        # and gets restarted reads as active here, so this is a precondition rather than
        # a result.
        - name: Assert the Postgres systemd services are active
          ansible.builtin.assert:
            that: postgres_services.results | map(attribute='status.ActiveState') | unique | list == ['active']
            fail_msg: >-
              Not every service is active:
              {{ dict(postgres_verify_units | zip(postgres_services.results | map(attribute='status.ActiveState'))) }}
            success_msg: "Every systemd service is active"

        - name: Retrieve the Postgres systemd services log for debugging
          when: postgres_debug_service_enabled | bool
          ansible.builtin.command:
            cmd: "journalctl --no-pager --output=cat --unit={{ item }}"
          changed_when: false
          loop: "{{ postgres_verify_units }}"

        - name: Verify nothing was quietly restarting while all of that was being checked
          when: "postgres_restarts_check_enabled | bool"
          block:
            # `Restart=always` means the unit reads `active` while it is crash-looping,
            # so `active` on its own says very little. The window has to be watched with
            # everything already answering.
            - name: Sample the restart counters
              ansible.builtin.systemd_service:
                name: "{{ item }}"
              register: postgres_restarts_before
              loop: "{{ postgres_verify_units }}"

            # Comfortably longer than the unit's `RestartSec=30`, so a crash loop has to
            # tick at least once inside the window.
            - name: Watch the services for 45 seconds
              ansible.builtin.pause:
                seconds: 45

            - name: Sample the restart counters again
              ansible.builtin.systemd_service:
                name: "{{ item }}"
              register: postgres_restarts_after
              loop: "{{ postgres_verify_units }}"

            - name: Assert no service restarted while it was up
              ansible.builtin.assert:
                that:
                  - postgres_restarts_after.results | map(attribute='status.NRestarts') | list == postgres_restarts_before.results | map(attribute='status.NRestarts') | list
                  - postgres_restarts_after.results | map(attribute='status.ActiveState') | unique | list == ['active']
                fail_msg: >-
                  A service restarted while it was supposedly up, so `active` was hiding a crash loop.
                  Before:
                  {% for unit, restarts in postgres_verify_units | zip(postgres_restarts_before.results | map(attribute='status.NRestarts')) %}{{ restarts }} ({{ unit }}){% if not loop.last %}, {% endif %}{% endfor %},
                  after:
                  {% for unit, restarts in postgres_verify_units | zip(postgres_restarts_after.results | map(attribute='status.NRestarts')) %}{{ restarts }} ({{ unit }}){% if not loop.last %}, {% endif %}{% endfor %}
                success_msg: >-
                  None of the services restarted over 45 seconds of watching (restarts so far:
                  {% for unit, restarts in postgres_verify_units | zip(postgres_restarts_after.results | map(attribute='status.NRestarts')) %}{{ restarts }} ({{ unit }}){% if not loop.last %}, {% endif %}{% endfor %})

        - name: Show the Postgres verification summary
          ansible.builtin.debug:
            msg:
              - "Postgres systemd: {% for unit, state in postgres_verify_units | zip(postgres_services.results | map(attribute='status.ActiveState')) %}{{ unit }} ({{ state }}){% if not loop.last %}, {% endif %}{% endfor %}"
              - "Postgres restarts: {% for unit, restarts in postgres_verify_units | zip(postgres_restarts_after.results | map(attribute='status.NRestarts')) %}{{ restarts }} ({{ unit }}){% if not loop.last %}, {% endif %}{% endfor %}"

      rescue:
        # Whatever went wrong, the reason is almost always in the systemd service log.
        - name: Retrieve the Postgres systemd service log
          ansible.builtin.command:
            cmd: journalctl --no-pager --output=cat --unit=postgres.service
          register: postgres_service_log
          changed_when: false

        - name: Show the Postgres systemd service log
          ansible.builtin.debug:
            var: postgres_service_log.stdout_lines

        - name: Fail because verification did not pass
          ansible.builtin.fail:
            msg: "Postgres verification failed. The systemd service log is printed above."
